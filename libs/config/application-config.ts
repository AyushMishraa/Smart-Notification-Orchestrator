const APP_NAME = 'Smart_Notification_Orchestrator';
const APP_ENV = process.env.NODE_ENV || 'development';
const APP_PORT = process.env.PORT || 3000;
const APP_VERSION = '1.0.0';

export { APP_NAME, APP_ENV, APP_PORT, APP_VERSION };